# En dev Vite usa proxy a localhost:3001 (ver vite.config.ts).
# En prod el backend sirve el frontend, así que esta variable no es necesaria.
# Solo defínela si despliegas frontend separado y apunta al backend remoto.

# VITE_API_BASE=http://localhost:3001
